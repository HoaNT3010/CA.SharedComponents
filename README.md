# Clean Architecture's Shared Components

**Shared Component** is a collection of libraries that bundled together as shared kernel layers to be reused as building block for projects that follow clean architecture.
